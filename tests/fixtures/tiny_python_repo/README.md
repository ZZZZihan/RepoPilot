# Tiny Calculator

`tinycalc` exposes a small `divide(dividend, divisor)` function.

Run the test suite with `python -m pytest`. Division by zero follows Python's normal
`ZeroDivisionError` behavior.
