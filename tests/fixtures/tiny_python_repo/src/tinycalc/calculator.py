"""Arithmetic operations used by the RepoPilot fixture."""


def divide(dividend: float, divisor: float) -> float:
    """Return ``dividend / divisor`` using Python's normal zero behavior."""

    return dividend / divisor
